import { html } from './http';
import { randomToken } from './crypto';

/** Administrative control center: publication, distribution and white-label are
 * intentionally managed here, not hidden behind ad-hoc links or source code. */
export function operationsUi(): Response {
  const nonce = randomToken(12);
  const markup = `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Central operacional · PJJ</title><style nonce="${nonce}">
:root{--ink:#12110f;--paper:#f6f4ee;--line:#e2ddd3;--gold:#bc8a29;--muted:#716c63;--bad:#a43d35;--ok:#176747}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:14px Inter,system-ui,sans-serif}.top{padding:20px max(4vw,20px);background:#090909;color:#fff;display:flex;justify-content:space-between;align-items:center;letter-spacing:.04em}.top a{color:#fff;text-decoration:none}.main{width:min(1320px,94vw);margin:30px auto 70px}.eyebrow{color:#9a6e19;font-size:11px;font-weight:800;letter-spacing:.17em;text-transform:uppercase}h1{font-size:clamp(30px,5vw,46px);letter-spacing:-.055em;margin:7px 0 10px}.intro{display:flex;justify-content:space-between;gap:25px;border-bottom:1px solid var(--line);padding-bottom:24px}.intro p{max-width:590px;line-height:1.55;color:var(--muted)}.cards{display:grid;grid-template-columns:repeat(4,1fr);gap:13px;margin:24px 0}.card,.panel,.form{background:#fff;border:1px solid var(--line);border-radius:15px;box-shadow:0 12px 30px #30241008}.card{padding:18px}.card span{color:var(--muted);font-size:12px}.card strong{font-size:30px;display:block;margin-top:8px}.tabs{display:flex;gap:7px;overflow:auto;padding:2px 0 17px}.tabs button,.btn{border:1px solid var(--line);background:#fff;color:#26231e;border-radius:9px;padding:10px 13px;font:inherit;font-weight:750;cursor:pointer;text-decoration:none}.tabs button{border-radius:99px;white-space:nowrap}.tabs button.active,.btn.dark{background:#12110f;color:white;border-color:#12110f}.btn.gold{background:var(--gold);color:#fff;border-color:var(--gold)}.btn.danger{background:var(--bad);color:#fff;border-color:var(--bad)}.panel{padding:23px;min-height:390px}.panel-head{display:flex;justify-content:space-between;gap:15px;align-items:start;margin-bottom:18px}.panel h2{margin:0;font-size:22px;letter-spacing:-.03em}.muted{color:var(--muted)}.grid{display:grid;grid-template-columns:1fr 1fr;gap:19px}.form{padding:18px}.form h3{margin:0 0 5px}.form p{margin:0 0 15px;line-height:1.45;color:var(--muted)}.fields{display:grid;grid-template-columns:1fr 1fr;gap:12px}.fields.one{grid-template-columns:1fr}label{font-size:12px;font-weight:750;display:grid;gap:6px}input,select,textarea{width:100%;padding:10px;border:1px solid #d8d1c4;border-radius:8px;background:#fff;font:inherit;color:#1b1916}textarea{min-height:76px;resize:vertical}.actions{display:flex;gap:7px;justify-content:flex-end;flex-wrap:wrap}.form-actions{margin-top:14px;display:flex;align-items:center;gap:10px}.msg{font-size:12px;color:var(--bad)}.table{border:1px solid var(--line);border-radius:11px;overflow:hidden}.row{display:grid;grid-template-columns:1.2fr .8fr .8fr 1fr;gap:12px;align-items:center;padding:13px 14px;border-bottom:1px solid var(--line)}.row:last-child{border:0}.row small{display:block;color:var(--muted);margin-top:3px}.tag{font-size:11px;font-weight:800;background:#eee7da;color:#604d20;padding:4px 7px;border-radius:99px}.tag.failed{background:#f8e2df;color:#92322d}.tag.ready{background:#e3f2e9;color:#176747}.empty{text-align:center;color:var(--muted);padding:48px 10px}.modal{position:fixed;inset:0;background:#0009;display:grid;place-items:center;padding:18px}.modal[hidden]{display:none}.modal-box{background:#fff;border-radius:16px;padding:24px;width:min(650px,100%)}.result{margin:13px 0;padding:12px;background:#f4f1e9;border-radius:8px;overflow-wrap:anywhere}.close{float:right;border:0;background:none;font-size:24px;cursor:pointer}@media(max-width:820px){.cards{grid-template-columns:repeat(2,1fr)}.intro{display:block}.grid{grid-template-columns:1fr}.row{grid-template-columns:1fr 1fr}.row .actions{grid-column:1/-1;justify-content:start}}@media(max-width:480px){.cards{grid-template-columns:1fr 1fr}.fields,.row{grid-template-columns:1fr}.panel{padding:16px}.main{width:92vw}}
</style></head><body><header class="top"><strong>PJJ · OPERAÇÃO</strong><a href="/admin">Voltar ao administrativo</a></header><main class="main"><div class="intro"><div><div class="eyebrow">Publicação, acesso e distribuição</div><h1>Central operacional</h1></div><p>Controle o que será entregue, crie acessos rastreáveis e revogue distribuições sem expor links públicos do Google Drive.</p></div><section id="summary" class="cards"></section><nav class="tabs"><button class="active" data-tab="jobs">Processamento</button><button data-tab="assets">Revisão</button><button data-tab="grants">Links privados</button><button data-tab="embeds">Embeds B2B</button><button data-tab="users">Convidar cliente</button><button data-tab="branding">Branding</button><button data-tab="sessions">Sessões</button><button data-tab="audit">Auditoria</button></nav><section class="panel"><div id="content">Carregando…</div></section></main><div id="modal" class="modal" hidden><div class="modal-box"><button class="close" data-close>×</button><h2 id="modalTitle">Acesso criado</h2><p id="modalText" class="muted"></p><div id="modalValue" class="result"></div><button class="btn gold" id="copy">Copiar</button></div></div><script nonce="${nonce}">
let csrf='',overview,access,audit,clients=[],projects=[],users=[],tab='jobs';const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),dt=v=>v?new Date(v).toLocaleString('pt-BR',{dateStyle:'short',timeStyle:'short'}):'Sem prazo',parse=v=>{try{return typeof v==='string'?JSON.parse(v):v||{}}catch{return{}}};
async function api(path,opt={}){opt.headers={'content-type':'application/json',...(csrf?{'x-csrf-token':csrf}:{}),...(opt.headers||{})};const r=await fetch(path,opt),d=await r.json().catch(()=>({}));if(!r.ok)throw Object.assign(new Error(d.error?.message||'Falha na operação'),{status:r.status});return d}const projectsOpt=()=>projects.map(p=>'<option value="'+p.id+'">'+esc(p.name)+' · '+esc(p.client_name)+'</option>').join(''),clientsOpt=()=>clients.map(c=>'<option value="'+c.id+'">'+esc(c.name)+'</option>').join(''),btn=(x,a,cl='')=>'<button class="btn '+cl+'" '+a+'>'+x+'</button>',empty=x=>'<div class="empty">'+esc(x)+'</div>';
function rows(items,kind){if(!items?.length)return empty('Nenhum registro ainda.');return '<div class="table">'+items.map(x=>{const state=x.status||x.permission||x.outcome||'—',detail=x.project_name||x.email||x.target_type||x.domains||'—',date=x.expires_at?'Expira '+dt(x.expires_at):dt(x.created_at),revoke=(kind==='grant'||kind==='embed'||kind==='session')&&!x.revoked_at?btn(kind==='session'?'Encerrar':'Revogar','data-revoke="'+kind+'" data-id="'+x.id+'"','danger'):'';return '<div class="row"><div><strong>'+esc(x.label||x.name||x.email||x.action||x.id)+'</strong><small>'+esc(detail)+'</small></div><div><span class="tag '+(state==='failed'?'failed':state==='published'||state==='active'?'ready':'')+'">'+esc(state)+'</span></div><div class="muted">'+esc(date)+'</div><div class="actions">'+revoke+'</div></div>'}).join('')+'</div>'}
function jobs(){const x=overview.jobs||[];return '<div class="panel-head"><div><h2>Processamento real</h2><p class="muted">O pipeline só libera um produto depois da sua revisão.</p></div>'+btn('Atualizar','data-refresh')+'</div>'+(x.length?'<div class="table">'+x.map(j=>'<div class="row"><div><strong>'+esc(j.asset_title)+'</strong><small>'+esc(j.type)+' · tentativa '+esc(j.attempt)+'/'+esc(j.max_attempts)+'</small></div><div><span class="tag '+(j.status==='failed'?'failed':j.status==='complete'?'ready':'')+'">'+esc(j.status)+'</span></div><div>'+esc(j.progress)+'%'+(j.error_message?'<small>'+esc(j.error_message)+'</small>':'')+'</div><div class="actions">'+(j.status==='failed'?btn('Reprocessar','data-retry="'+j.id+'"','dark'):'')+'</div></div>').join('')+'</div>':empty('A fila está vazia.'))}
function assets(){const x=overview.assets||[];return '<div class="panel-head"><div><h2>Revisão antes da publicação</h2><p class="muted">Produtos em revisão não aparecem para o cliente até sua aprovação.</p></div></div>'+(x.length?'<div class="table">'+x.map(a=>'<div class="row"><div><strong>'+esc(a.title)+'</strong><small>'+esc(a.project_name)+' · '+esc(a.type)+'</small></div><div><span class="tag '+(a.status==='published'?'ready':a.status==='failed'?'failed':'')+'">'+esc(a.status)+'</span></div><div class="muted">'+dt(a.created_at)+'</div><div class="actions">'+(a.status==='review'?'<a class="btn gold" href="/admin/review/'+a.id+'">Revisar</a>':'')+(a.status==='published'?'<a class="btn" href="/viewer/'+a.id+'">Abrir</a>':'')+btn('Lixeira','data-trash="'+a.id+'"','danger')+'</div></div>').join('')+'</div>':empty('Nenhum produto registrado.'))}
function grants(){return '<div class="grid"><form id="grantForm" class="form"><h3>Novo link privado</h3><p>Prazo, PIN e limite de usos; o arquivo não se torna público no Drive.</p><div class="fields one"><label>Projeto<select name="projectId" required>'+projectsOpt()+'</select></label><label>Identificação interna<input name="label" placeholder="Ex.: diretoria setembro"></label></div><div class="fields"><label>Permissão<select name="permission"><option value="view">Visualizar</option><option value="download">Visualizar e baixar</option></select></label><label>Expira em<input type="datetime-local" name="expiresAt"></label><label>PIN opcional<input name="pin" inputmode="numeric" pattern="[0-9]{4,10}" placeholder="4 a 10 dígitos"></label><label>Máximo de acessos<input name="maxUses" type="number" min="1" placeholder="Ilimitado"></label></div><div class="form-actions">'+btn('Criar link seguro','type="submit"','gold')+'<span class="msg"></span></div></form><div class="form"><h3>Links em circulação</h3><p>Revogue imediatamente quando o acesso não deve mais funcionar.</p>'+rows(access.grants,'grant')+'</div></div>'}
function embeds(){return '<div class="grid"><form id="embedForm" class="form"><h3>Novo embed controlado</h3><p>Viewer vinculado a domínios permitidos, produto e prazo de expiração.</p><div class="fields one"><label>Projeto<select name="projectId" required>'+projectsOpt()+'</select></label><label>Nome interno<input name="name" required placeholder="Site do cliente"></label><label>Domínios autorizados<textarea name="domains" required placeholder="cliente.com.br&#10;www.cliente.com.br"></textarea></label></div><div class="fields"><label>Apresentação<select name="mode"><option value="pjj">PJJ Produções</option><option value="white_label">Marca do cliente</option></select></label><label>Nome no cabeçalho<input name="brandName" placeholder="Opcional"></label><label>Cor principal<input type="color" name="accent" value="#bc8a29"></label><label>Produtos permitidos<input name="products" placeholder="ortofoto, modelo_3d"></label><label>Expira em<input type="datetime-local" name="expiresAt"></label></div><div class="form-actions">'+btn('Criar embed','type="submit"','gold')+'<span class="msg"></span></div></form><div class="form"><h3>Embeds ativos</h3><p>O acesso responde apenas aos domínios que você autorizou.</p>'+rows(access.embeds,'embed')+'</div></div>'}
function invite(){return '<div class="grid"><form id="userForm" class="form"><h3>Convidar cliente ao portal</h3><p>O convite é individual e expira em sete dias.</p><div class="fields one"><label>Nome<input name="displayName" required></label><label>E-mail<input name="email" type="email" required></label><label>Projetos permitidos<select name="projectIds" multiple size="5" required>'+projectsOpt()+'</select></label><label>Permissão<select name="permission"><option value="view">Visualizar</option><option value="download">Visualizar e baixar</option></select></label></div><div class="form-actions">'+btn('Gerar convite','type="submit"','gold')+'<span class="msg"></span></div></form><div class="form"><h3>Clientes convidados</h3><p>Convites e contas do portal.</p>'+rows(users,'user')+'</div></div>'}
function branding(){const list=clients.map(c=>{const b=parse(c.branding_json);return{...c,label:c.name,project_name:b.name||'Marca PJJ padrão',status:b.accent||'sem personalização'}});return '<div class="grid"><form id="brandForm" class="form"><h3>Identidade do cliente</h3><p>Configure a marca que poderá ser usada nos embeds white-label.</p><div class="fields one"><label>Cliente<select name="clientId" required>'+clientsOpt()+'</select></label><label>Nome exibido<input name="name" placeholder="Nome da marca"></label><label>Cor de destaque<input name="accent" type="color" value="#bc8a29"></label><label>Logo (URL opcional)<input name="logo" type="url" placeholder="https://…"></label></div><div class="form-actions">'+btn('Salvar identidade','type="submit"','gold')+'<span class="msg"></span></div></form><div class="form"><h3>Marcas configuradas</h3><p>Ao criar um embed, escolha “Marca do cliente” para aplicar esta identidade sem perder o controle de domínio.</p>'+rows(list,'brand')+'</div></div>'}
function view(){const content={jobs,assets,grants,embeds,users:invite,branding,sessions:()=>'<div class="panel-head"><div><h2>Sessões ativas</h2><p class="muted">Encerre acessos imediatamente.</p></div></div>'+rows(access.sessions,'session'),audit:()=>'<div class="panel-head"><div><h2>Rastro de auditoria</h2><p class="muted">Ações administrativas e de distribuição.</p></div>'+btn('Atualizar','data-refresh')+'</div>'+rows(audit.items,'audit')};$('#content').innerHTML=content[tab]();document.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));bind()}
function status(f,t,b=false){const n=f.querySelector('.msg');n.textContent=t;n.style.color=b?'var(--bad)':'var(--ok)'}function result(title,text,value){$('#modalTitle').textContent=title;$('#modalText').textContent=text;$('#modalValue').textContent=value;$('#modal').hidden=false}function bind(){document.querySelectorAll('[data-refresh]').forEach(b=>b.onclick=load);document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>$('#modal').hidden=true);$('#copy')?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText($('#modalValue').textContent);$('#copy').textContent='Copiado'}catch{$('#copy').textContent='Copie manualmente'}});document.querySelectorAll('[data-revoke]').forEach(b=>b.onclick=async()=>{if(!confirm('Revogar este acesso agora?'))return;try{await api('/api/admin/'+b.dataset.revoke+'s/'+b.dataset.id+'/revoke',{method:'POST'});load()}catch(e){alert(e.message)}});document.querySelectorAll('[data-retry]').forEach(b=>b.onclick=async()=>{try{await api('/api/admin/jobs/'+b.dataset.retry+'/retry',{method:'POST'});load()}catch(e){alert(e.message)}});document.querySelectorAll('[data-trash]').forEach(b=>b.onclick=async()=>{if(!confirm('Mover para lixeira? A recuperação continua possível.'))return;try{await api('/api/admin/assets/'+b.dataset.trash,{method:'DELETE'});load()}catch(e){alert(e.message)}});$('#grantForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=e.currentTarget,d=Object.fromEntries(new FormData(f));if(!d.pin)delete d.pin;if(!d.expiresAt)delete d.expiresAt;if(d.maxUses)d.maxUses=Number(d.maxUses);else delete d.maxUses;try{const r=await api('/api/admin/grants',{method:'POST',body:JSON.stringify(d)});result('Link privado criado','Copie agora: por segurança, ele não é exibido novamente.',r.url);load()}catch(x){status(f,x.message,true)}});$('#embedForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=e.currentTarget,d=Object.fromEntries(new FormData(f));d.domains=String(d.domains).split(/[\\n,; ]+/).filter(Boolean);d.products=String(d.products||'').split(/[\\n,;]+/).filter(Boolean);if(!d.expiresAt)delete d.expiresAt;try{const r=await api('/api/admin/embeds',{method:'POST',body:JSON.stringify(d)});result('Embed criado','Use este endereço apenas no iframe dos domínios liberados.',r.url);load()}catch(x){status(f,x.message,true)}});$('#userForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=e.currentTarget,d=Object.fromEntries(new FormData(f));d.projectIds=[...f.projectIds.selectedOptions].map(o=>o.value);try{const r=await api('/api/admin/users',{method:'POST',body:JSON.stringify(d)});result('Convite criado','Envie este link ao cliente; ele expira em sete dias.',r.inviteUrl);load()}catch(x){status(f,x.message,true)}});$('#brandForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=e.currentTarget,d=Object.fromEntries(new FormData(f)),c=clients.find(x=>x.id===d.clientId);if(!c)return;const b={...parse(c.branding_json),name:String(d.name||'').trim()||c.name,accent:d.accent,logo:String(d.logo||'').trim()||undefined};try{await api('/api/admin/clients/'+d.clientId,{method:'PATCH',body:JSON.stringify({branding_json:b})});status(f,'Identidade salva para próximos embeds.');load()}catch(x){status(f,x.message,true)}})}
async function load(){try{const r=await Promise.all([api('/api/admin/overview'),api('/api/admin/access'),api('/api/admin/audit?limit=100'),api('/api/admin/clients?limit=100'),api('/api/admin/projects?limit=100'),api('/api/admin/users')]);[overview,access,audit]=r;clients=r[3].items||[];projects=r[4].items||[];users=r[5].items||[];const c=overview.counts||{};$('#summary').innerHTML=[['Clientes ativos',c.clients||0],['Projetos',c.projects||0],['Produtos',c.assets||0],['Usuários ativos',c.users||0]].map(x=>'<div class="card"><span>'+x[0]+'</span><strong>'+x[1]+'</strong></div>').join('');view()}catch(e){$('#content').innerHTML=empty(e.message||'Não foi possível carregar.');if(e.status===401)setTimeout(()=>location.href='/admin',500)}}document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;view()});api('/api/auth/me').then(x=>{csrf=x.csrfToken;load()}).catch(()=>location.href='/admin');
</script></body></html>`;
  return html(markup, nonce);
}
